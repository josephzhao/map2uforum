
ccdn_forum_moderator_topic_close:
    pattern: /{topicId}/close
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:close, _locale: en, forumName: default }

ccdn_forum_moderator_topic_reopen:
    pattern: /{topicId}/reopen
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:reopen, _locale: en, forumName: default }

ccdn_forum_moderator_topic_sticky:
    pattern: /{topicId}/sticky
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:sticky, _locale: en, forumName: default }

ccdn_forum_moderator_topic_unsticky:
    pattern: /{topicId}/unsticky
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:unsticky, _locale: en, forumName: default }

ccdn_forum_moderator_topic_delete:
    pattern: /{topicId}/delete
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:delete, _locale: en, forumName: default }

ccdn_forum_moderator_topic_delete_process:
    pattern: /{topicId}/delete/process
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:deleteProcess, _locale: en, forumName: default }

ccdn_forum_moderator_topic_restore:
    pattern: /{topicId}/restore
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:restore, _locale: en, forumName: default }

ccdn_forum_moderator_topic_change_board:
    pattern: /{topicId}/change-board
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:changeBoard, _locale: en, forumName: default }

ccdn_forum_moderator_topic_change_board_process:
    pattern: /{topicId}/change-board/process
    defaults: { _controller: Map2uForumBundle:ModeratorTopic:changeBoardProcess, _locale: en, forumName: default }
